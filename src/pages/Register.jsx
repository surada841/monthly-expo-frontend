import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import api from '../api/api';
export default function Register(){const [form,setForm]=useState({name:'',mobile:'',email:'',password:''}); const nav=useNavigate(); const submit=async e=>{e.preventDefault(); await api.post('/auth/register/initiate',form); localStorage.setItem('regMobile',form.mobile); nav('/verify-otp');}; return <div className="auth-box card p-4"><h3>Register</h3><form onSubmit={submit}>{['name','mobile','email','password'].map(x=><input key={x} className="form-control my-2" type={x==='password'?'password':'text'} placeholder={x} onChange={e=>setForm({...form,[x]:e.target.value})}/>) }<button className="btn btn-success w-100">Send OTP</button></form></div>}
