import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import api from '../api/api';
export default function VerifyOtp(){const [otp,setOtp]=useState(''); const nav=useNavigate(); const mobile=localStorage.getItem('regMobile'); const submit=async e=>{e.preventDefault(); await api.post('/auth/register/verify',{mobile,otp}); nav('/login');}; return <div className="auth-box card p-4"><h3>Verify OTP</h3><p>Dev OTP is 123456</p><form onSubmit={submit}><input className="form-control my-2" placeholder="OTP" onChange={e=>setOtp(e.target.value)}/><button className="btn btn-primary w-100">Verify</button></form></div>}
